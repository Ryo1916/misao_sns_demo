<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
  <% unless @post.image_name.blank? %>
    <%= image_tag(@post.image_name, width: "100px", height: "100px") %>
  <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
